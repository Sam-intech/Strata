# Tooling for tests, evaluation scripts, training and notebooks.
# Includes the runtime dependencies from requirements.txt.
-r requirements.txt

# tests (FastAPI's TestClient uses httpx, pinned in requirements.txt)
pytest==9.0.2

# evaluation plots + notebooks
matplotlib==3.10.8
jupyterlab==4.5.1
notebook==7.5.1
ipykernel==7.1.0
ipywidgets==8.1.8
